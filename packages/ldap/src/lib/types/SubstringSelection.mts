/**
 * @summary Type of a substrings match
 * @description
 * 
 * Used for substrings matching rules.
 * 
 * @enum
 */
export
enum SubstringSelection {
    any_,
    initial,
    final,
};

export default SubstringSelection;
