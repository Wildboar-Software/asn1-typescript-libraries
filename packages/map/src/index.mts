/**
 * @packageDocumentation
 *
 * TypeScript encodings of GSM/UMTS Mobile Application Part (MAP) ASN.1
 * modules from 3GPP TS 29.002. Import from a subpath (for example
 * `@wildboar/map/MAP-DialogueInformation`) rather than this root: the
 * package root does not re-export every module.
 *
 * Remote Operations types used by MAP (`OPERATION`, `ERROR`, `Code`,
 * `Priority`) are re-exported from `@wildboar/rose`.
 */
export {};
