# csta

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build csta` to build the library.

## Running unit tests

Run `nx test csta` to execute the unit tests via [Vitest](https://vitest.dev/).
