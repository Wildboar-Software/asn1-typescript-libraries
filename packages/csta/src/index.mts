/**
 * @packageDocumentation
 *
 * TypeScript encodings of ECMA-269 CSTA Phase III ASN.1 modules.
 * Import from a subpath (for example `@wildboar/csta/CSTA-make-call`)
 * rather than this root: the package root does not re-export every
 * module.
 *
 * Remote Operations types used by CSTA (`OPERATION`, `ERROR`, `Code`,
 * `Priority`) are re-exported from `@wildboar/rose`.
 */
export {};
