/**
 * @summary SecurityCategoriesTable
 * @description
 *
 * Object set of `SECURITY-CATEGORY`. Extensible and empty in the
 * Recommendation (`{... -- objects defined as needed --}`). The
 * `type` component of `SecurityCategory` selects a row; `value`
 * is that row's `&Type`. Re-exported from `@wildboar/x500`. ITU-T
 * Rec. X.841 (10/2000)
 * [§6.1.2](https://www.itu.int/rec/T-REC-X.841-200010-I), Annex B.
 */
export { SecurityCategoriesTable } from "@wildboar/x500/EnhancedSecurity";
