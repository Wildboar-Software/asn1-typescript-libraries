/* eslint-disable */
import { TYPE_IDENTIFIER } from "@wildboar/asn1";

/**
 * @summary OC_DATA
 * @description
 *
 * `TYPE-IDENTIFIER` (`&id`, `&Type`) for optional-category data in
 * `OptionalCategoryData`. `CatData` is the object set (extensible,
 * initially empty). ITU-T Rec. X.841 (10/2000)
 * [§6.2.2.6](https://www.itu.int/rec/T-REC-X.841-200010-I).
 */
export type OC_DATA = TYPE_IDENTIFIER;

/* eslint-enable */
