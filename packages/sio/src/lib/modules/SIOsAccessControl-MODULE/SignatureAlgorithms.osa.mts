/* eslint-disable */
import { type ALGORITHM } from "../SIOsAccessControl-MODULE/ALGORITHM.oca.mjs";

/**
 * @summary SignatureAlgorithms
 * @description
 *
 * Object set of `ALGORITHM` for `SIGNED` on a SPIF. Extensible
 * and empty in the Recommendation (`{...}`); implementations
 * populate it as needed. ITU-T Rec. X.841 (10/2000) Annex A.
 *
 * ### ASN.1 Definition:
 *
 * ```asn1
 * SignatureAlgorithms ALGORITHM ::= {... -- defined as needed --}
 * ```
 *
 * @constant
 * @type {ALGORITHM[]}
 *
 */
export const SignatureAlgorithms: ALGORITHM[] = [];

/* eslint-enable */
