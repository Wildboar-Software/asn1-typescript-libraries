# mms

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build mms` to build the library.

## Running unit tests

Run `nx test mms` to execute the unit tests via [Vitest](https://vitest.dev/).
