# dfr

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build dfr` to build the library.

## Running unit tests

Run `nx test dfr` to execute the unit tests via [Vitest](https://vitest.dev/).
