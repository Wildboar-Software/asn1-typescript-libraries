/**
 * @packageDocumentation
 *
 * TypeScript encodings of ISO/IEC 10166 Document Filing and Retrieval
 * (DFR) ASN.1 modules. Import from a subpath (for example
 * `@wildboar/dfr/DFRAbstractService`) rather than this root: the
 * package root does not re-export every module.
 *
 * Directory types (`Attribute`, `Filter`, `DistinguishedName`) are
 * re-exported from `@wildboar/x500`. Remote Operations types
 * (`OPERATION`, `ERROR`, `Code`, `InvokeId`) are re-exported from
 * `@wildboar/rose`. `PresentationAddress` is imported from
 * `@wildboar/x500/SelectedAttributeTypes`.
 */
export {};
