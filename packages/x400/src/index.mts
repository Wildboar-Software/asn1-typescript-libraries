/**
 * @packageDocumentation
 *
 * TypeScript encodings of the X.400 Message Handling System ASN.1 modules
 * (ITU-T F.400/X.400, X.402, X.411, X.412, X.413, X.419, X.420, and X.435,
 * June 1999). Import from a subpath (for example
 * `@wildboar/x400/MTSAbstractService`) rather than this root: the package root
 * does not re-export every module.
 *
 * O/R address types that also exist in `@wildboar/or-address` (for example
 * `ORAddress`, `PersonalName`, `CountryName`) are re-exported from that
 * package. See ITU-T X.402 (1999) §18 for address semantics.
 */
export {};
