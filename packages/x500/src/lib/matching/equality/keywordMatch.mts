export {
    caseIgnoreMatch as keywordMatch,
    caseIgnoreMatchTyped as keywordMatchTyped,
} from "./caseIgnoreMatch.mjs";

import { caseIgnoreMatch } from "./caseIgnoreMatch.mjs";

export default caseIgnoreMatch;
