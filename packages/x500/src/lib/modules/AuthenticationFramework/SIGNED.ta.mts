/**
 * Signed wrapper (`toBeSigned` + algorithm + BIT STRING). Re-exported from
 * `@wildboar/pki-stub` (DER-over-toBeSigned and alt-signature rules there).
 */
export {
    SIGNED,
    _get_decoder_for_SIGNED,
    _get_encoder_for_SIGNED,
} from "@wildboar/pki-stub";
