/* eslint-disable */
import { type SECURITY_CATEGORY } from "../EnhancedSecurity/SECURITY-CATEGORY.oca.mjs";
/**
 * @summary SecurityCategoriesTable
 * @description
 *
 * Object set of {@link SECURITY_CATEGORY}: extensible, initially empty
 * `{...}`. Empty means no standardized categories; implementations populate
 * locally. Used to interpret {@link SecurityCategory} `type`/`value`.
 *
 * ### ASN.1 Definition:
 *
 * ```asn1
 * SecurityCategoriesTable SECURITY-CATEGORY ::= {...}
 * ```
 *
 * @constant
 * @type {SECURITY_CATEGORY[]}
 *
 */
export const SecurityCategoriesTable: SECURITY_CATEGORY[] = [];

/* eslint-enable */
