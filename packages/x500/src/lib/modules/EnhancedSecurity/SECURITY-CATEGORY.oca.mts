/* eslint-disable */
import { TYPE_IDENTIFIER } from "@wildboar/asn1";

/**
 * @summary SECURITY-CATEGORY
 * @description
 *
 * TYPE-IDENTIFIER information object class (`&id`, `&Type`) for
 * {@link SecurityCategory} open-type values. {@link SecurityCategoriesTable}
 * is the object set (extensible, initially empty `{...}`).
 */
export type SECURITY_CATEGORY = TYPE_IDENTIFIER;

/* eslint-enable */
