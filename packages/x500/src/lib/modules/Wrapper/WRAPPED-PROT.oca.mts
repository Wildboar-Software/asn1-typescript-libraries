/* eslint-disable */
import { TYPE_IDENTIFIER } from "@wildboar/asn1";

/**
 * @summary WRAPPED_PROT
 * @description
 *
 * TYPE-IDENTIFIER class binding a protected-protocol OID (`&id`) to that
 * protocol’s top-level PrPDU CHOICE (`&Type`).
 */
export type WRAPPED_PROT = TYPE_IDENTIFIER;

/* eslint-enable */
