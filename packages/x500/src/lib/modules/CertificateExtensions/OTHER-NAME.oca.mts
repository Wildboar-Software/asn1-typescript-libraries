/**
 * Re-export of {@link OTHER_NAME} from `@wildboar/pki-stub`.
 *
 * TYPE-IDENTIFIER for GeneralName `otherName` (`INSTANCE OF OTHER-NAME`).
 * Unbounded otherName forms cannot all be constrained by nameConstraints.
 * Critical containing extension: at least one recognized name form required.
 */
export type {
    OTHER_NAME,
} from "@wildboar/pki-stub";
