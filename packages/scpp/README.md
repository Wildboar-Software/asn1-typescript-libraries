# scpp

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build scpp` to build the library.

## Running unit tests

Run `nx test scpp` to execute the unit tests via [Vitest](https://vitest.dev/).
