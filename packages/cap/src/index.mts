/**
 * @packageDocumentation
 *
 * TypeScript encodings of CAMEL Application Part (CAP) ASN.1 modules from
 * 3GPP TS 29.078. Import from a subpath (for example
 * `@wildboar/cap/CAP-datatypes`) rather than this root: the package root
 * does not re-export every module.
 */
export {};
