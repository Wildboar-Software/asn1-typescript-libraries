# cap

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build cap` to build the library.

## Running unit tests

Run `nx test cap` to execute the unit tests via [Vitest](https://vitest.dev/).
