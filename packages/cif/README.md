# cif

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build cif` to build the library.

## Running unit tests

Run `nx test cif` to execute the unit tests via [Vitest](https://vitest.dev/).
